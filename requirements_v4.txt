# JARVIS v4 — Python dependencies
# =================================

# Core (stdlib-only, listed for documentation)
# json, time, math, os, re, base64, logging, unicodedata, urllib, traceback

# Numerical computing (required)
numpy>=1.24.0

# Embedding-based vector store (Upgrade 3)
# Falls back to TF-IDF if not installed
sentence-transformers>=2.2.0

# HTTP requests (used by Tavily/SerpAPI if requests is preferred over urllib)
# urllib.request is used by default; requests is optional
# requests>=2.28.0

# Testing
pytest>=7.0.0
